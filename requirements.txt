# Elasticsearch client, upgraded from <6.0.0 to >=8.0.0
elasticsearch>=8.0.0,<9.0.0

# HTTP and HTML parsing
requests>=2.32.0
beautifulsoup4>=4.13.0
lxml>=5.4.0

# NLP and Sentiment
nltk>=3.9.0
textblob>=0.19.0
vaderSentiment>=3.3.2
newspaper3k>=0.2.8

# Twitter API (v2 supported)
tweepy>=4.15.0

# Optional but useful for parsing and feeds
cssselect>=1.3.0
feedparser>=6.0.0
feedfinder2>=0.0.4

# Optional: for cleaning up lxml HTML safely
lxml_html_clean==0.1.1  # if you still use it manually; otherwise omit
